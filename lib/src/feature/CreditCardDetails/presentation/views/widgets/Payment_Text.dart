import 'package:flutter/material.dart';


class Payment_Text extends StatelessWidget {
  const Payment_Text({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text(
      'Payment link not used',
      style: TextStyle(
        color: const Color(0xFF295488),
        fontSize: 14,
        fontFamily: 'Inter',
        fontWeight: FontWeight.w500,
      ),
    );
  }
}